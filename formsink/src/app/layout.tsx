import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"),
  title: {
    default: "FormSink — Form backend for static sites",
    template: "%s — FormSink",
  },
  description:
    "Point your HTML form at a FormSink endpoint and get submissions in your inbox. No server, no code, no maintenance. Free for 50 submissions a month.",
  openGraph: {
    title: "FormSink — Form backend for static sites",
    description:
      "Point your HTML form at a FormSink endpoint and get submissions in your inbox. No server required.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
